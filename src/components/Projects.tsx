import React from "react"
import { Typography } from "@mui/material"

export default function Projects() {
    return (
        <div style={{ padding: 10 }}>
            <Typography>
            Coming sooner or later... (Projects)
            </Typography>
        </div>
    )
}
