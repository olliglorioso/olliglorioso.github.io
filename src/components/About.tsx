import { Typography } from "@mui/material";
import React from "react";

export default function About() {
    return <div>
        <br />
        <Typography>
            Coming sooner or later... (About)
        </Typography>
    </div>
}